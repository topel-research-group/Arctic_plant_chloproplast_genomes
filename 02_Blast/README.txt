#folder contains results of sample contigs vs full chloroplast
#distribution of these contigs was analized in R (need to add folder) and on this bases we decided to extract all contigs longer than 1000 bp and 65% of its length hit for the annotation step.
extract_chloroplast_contigs.sh #extracts all contigs longer than 1000bp and 50% cover and over than 65% of its length hit) and writes them to a new file results in dir:"chloroplast_contigs_rapid_genomics_run2" 
